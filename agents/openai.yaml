interface:
  display_name: "Project Director"
  short_description: "Coordinate long-running project work with director mode"
  default_prompt: "Use $project-director to coordinate this project with the lightest useful mode, bounded subagent groups when helpful, persistent memory when needed, and verified final delivery."
